2023-10-17 패스트 캠퍼스 
            한번에 끝내는 Java/Spring 웹 개발 마스터 초격차 패키지 Online
                    Part 4-1 안정적인 서비스를 위한 스프링 DATA와 Security - JPA 프로그래밍
                        Ch 07.영속성이란?
                          02.Entity 캐시
                            Entity 캐시

                            EntityManager 의 createQuery : 쿼리를 만들어서 실행시켜줌

                            ID조회진행시 → Entity캐시에 해당정보가 있는지 확인 후 있으면 넘겨줌(1차캐시) → 없다면 실제 쿼리로 진행

                            flush → 영속성캐시를 비워주는 메서드

                            영속성캐시가 flush가 되어서 DB에 반영되는시점
                            1.메서드를 명시적으로 호출하는시점
                            2.commit되는시점
                            3.복잡한 조회조건에 JPA쿼리가 실행되는시점
            Udemy
               JavaScript 완벽 가이드 : 초급 + 고급 마스터 과정
                    58.자동완성 & IDE 힌트로 작업하기
                        IDE는 자동적으로 자동완성기능을 지원한다
                        해당 자동완성기능 옆에 해당 함수의 설명도 제공한다
                        객체의 . 을 입력하면 사용할 수 있는 메서드와 함수를 보여준다
                            해당 자동완성은 ctrl + 스페이스로 호출가능하다
                        함수를 호출 후 괄호를 입력하면 넣어야하는 매개변수를 보여준다
                            해당 자동완성은 shift + ctrl + 스페이스로 호출가능하다